# TASKS-021 — Analítica multiserie y zonas

- [x] T1 Crear esquema `sensor_zones` y relación multitenant.
- [x] T2 Implementar catálogo jerárquico tipo/zona/dispositivo.
- [x] T3 Implementar consulta histórica agregada con límites.
- [x] T4 Implementar selector de zona y sensores.
- [x] T5 Implementar widget y selector de diez tipos de gráfico.
- [x] T6 Crear catálogo y telemetría sintética de demostración.
- [x] T7 Añadir pruebas contractuales y anti-IDOR del backend. *(2026-09-02: cerrado — `resolveAllowedSensorTenant` se extrajo a `backend/src/mining/sensor_tenant_resolver.cpp` (unidad de compilación propia, sin la cadena OpenCV/ONNX que bloqueaba el intento del 2026-08-30) y `backend/tests/test_sensor_anti_idor.cpp` ya compila y corre en el target liviano `beemetry_backend_tests`. Verificado con `docker build -f backend/Dockerfile.verify` limpio (sin cache): 742 aserciones en 50 test cases, 100% passed, incluidas las 4 del guard anti-IDOR (sesión sin `tenant_id`, `tenant_id` propio, `tenant_id` vacío, y el caso real de IDOR — tenant ajeno sin membresía real → fail-closed). La rama de acceso cruzado REAL con Postgres vivo (`userBelongsToTenant` consultando `auth_user_tenant`) sigue sin cubrir por este test unitario — solo se verifica que sin BD el resultado es fail-closed, no que la concesión real funcione con datos reales.)*
- [x] T8 Validar visualmente los diez tipos y estados vacíos. *(2026-09-12: el catálogo real creció a 20 tipos (`CHART_TYPE_LABELS`), no 10 — `SensorMultiChartWidget.smoke.test.tsx`, 41/41 passed: monta los 20 tipos sin selección (estado vacío) y 19/20 con sensores reales + datos mock (excluido "geomap" con marcadores por una limitación de jsdom+Leaflet+Canvas en el teardown, no un bug real — ver comentario en el test), más modo combinado y modo `isPrint`. Encontrado de paso: jsdom no implementa `ResizeObserver`, agregado un stub a `setupTests.js`. No sustituye una revisión visual real en navegador — cierra la parte automatizable (ningún tipo rompe el montaje).)* |
- [x] T9 Verificar exportación PDF/PPTX y unidades/ejes. *(2026-09-13, ver ADR-182: generado y verificado un PDF (378 pág., ~4m38s) y un PPTX (378 diapositivas, ~4m39s) reales contra el stack completo, sobre un informe QA ya existente con los 20 tipos (56 instancias c/u). 12/20 tipos correctos sin hallazgos; 2 bugs reales encontrados y corregidos con test de regresión (`SensorMultiChartWidget.histogram.test.tsx`, 42/42 passed): `histogram` mostraba las 10 etiquetas de bucket idénticas "0.0–0.0" para sensores de magnitud pequeña (decimales ahora derivados del ancho real del bucket), y `pie`/`donut`/`funnel` recortaban el nombre del sensor a mitad de palabra en el layout de export (ahora truncan con "…" vía `label.overflow`). `sunburst` queda con un hallazgo de solapamiento de etiquetas documentado, sin corregir (alcance acotado, ver ADR-182).)* |
- [x] T10 Incorporar la suite al gate CI y registrar evidencia E2E. *(2026-09-12: `.github/workflows/ci.yml` ya corre `npm run test:run --if-present` sin condición en cada push/PR — el nuevo `SensorMultiChartWidget.smoke.test.tsx` queda automáticamente incluido, sin wiring adicional. "Evidencia E2E" real con Playwright contra un navegador queda pendiente — ver T9.)* |
